//A clip has a current text component, and an audio component.  

//An audio component has an audio file and a text component of what the audio actually is

//Dirty flag -> this.text == this.audio.text

